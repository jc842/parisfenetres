---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const posts = await getCollection('blog-es');
---

<Layout title="Paris Fenêtres — Ventanas Haussmannianas y Aislamiento Acústico en París" lang="es">
  <section class="border-b border-stone-800 bg-stone-950 py-16">
    <div class="mx-auto max-w-7xl px-4 sm:px-6">
      <div class="inline-flex items-center gap-2 rounded-full border border-amber-500/30 bg-amber-950/30 px-3.5 py-1 text-xs text-amber-300 mb-4">
        EDICIÓN EN ESPAÑOL
      </div>
      <h1 class="font-serif text-3xl font-extrabold text-stone-100 sm:text-5xl">Ventanas Haussmannianas y Aislamiento en París</h1>
      <p class="mt-4 max-w-2xl text-stone-300 text-sm sm:text-base leading-relaxed font-light">
        Guía técnica de carpintería tradicional francesa, normativa ABF y doble acristalamiento acústico en apartamentos parisinos.
      </p>
    </div>
  </section>

  <section class="py-12 bg-stone-950">
    <div class="mx-auto max-w-7xl px-4 sm:px-6">
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
        {posts.map(post => (
          <article class="flex flex-col justify-between rounded-xl border border-stone-800 bg-stone-900/50 p-6 hover:border-amber-500/50 transition-colors">
            <div>
              <span class="rounded bg-stone-800 px-2 py-0.5 text-[11px] text-amber-400 font-semibold">{post.data.category}</span>
              <h2 class="font-serif text-lg font-bold text-stone-100 mt-3 hover:text-amber-400 transition-colors">
                <a href={'/es/' + post.id + '/'}>{post.data.title}</a>
              </h2>
              <p class="mt-2 text-xs text-stone-400 leading-relaxed">
                {post.data.description}
              </p>
            </div>
            <div class="mt-6 pt-4 border-t border-stone-800 text-xs">
              <a href={'/es/' + post.id + '/'} class="text-amber-400 hover:underline">Leer Guía →</a>
            </div>
          </article>
        ))}
      </div>
    </div>
  </section>
</Layout>
